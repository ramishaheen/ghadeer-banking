# Copy to .env and fill in.

DATABASE_URL="file:./dev.db"

# Optional — powers Rafiq's conversational intelligence (claude-opus-4-8).
# Without it, the deterministic rules engine answers chat instead.
ANTHROPIC_API_KEY=""
